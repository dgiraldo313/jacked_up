

<%= form_for(@workout) do |f| %>
  <% if @workout.errors.any? %>
    <div id= "error-explanation">
      <h2><%= pluralize(@workout.errors.count, "error") %> prohibited this workouts from being saved:</h2>

      <ul>
      <% @workout.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class= "field">
    <%= f.text_field :spades,placeholder: 'Spades' %>
  </div>
  <div class= "field">
    <%= f.text_field :hearts,placeholder: 'Hearts' %>
  </div>
  <div class= "field">
    <%= f.text_field :clubs,placeholder: 'Clubs' %>
  </div>
  <div class= "field">
    <%= f.text_field :diamonds,placeholder: 'Diamonds' %>
  </div>
  <div class= "submit">
    <%= f.submit %>
  </div>

<% end %>
