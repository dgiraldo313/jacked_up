
  <% if @history.errors.any? %>
    <div>
      <h2><%= pluralize(@history.errors.count, "error") %> prohibited this historys from being saved:</h2>

      <ul>
      <% @history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div >
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div >
    <%= f.submit %>
  </div>
